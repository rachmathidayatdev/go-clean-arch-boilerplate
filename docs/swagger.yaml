basePath: /
definitions:
  library.HTTPResponse:
    properties:
      data:
        type: object
      message:
        type: object
      status_code:
        type: string
    type: object
host: localhost:4747
info:
  contact:
    email: support@swagger.io
    name: API Support
    url: http://www.swagger.io/support
  description: This is a sample server Petstore server.
  license:
    name: Apache 2.0
    url: http://www.apache.org/licenses/LICENSE-2.0.html
  termsOfService: http://swagger.io/terms/
  title: Swagger Example API
  version: "1.0"
paths:
  /health:
    get:
      consumes:
      - '*/*'
      description: get the status of server.
      produces:
      - application/json
      responses:
        "200":
          description: OK
          schema:
            $ref: '#/definitions/library.HTTPResponse'
      summary: Show the status of server.
schemes:
- http
swagger: "2.0"
